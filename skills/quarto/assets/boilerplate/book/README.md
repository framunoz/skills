# Quarto Book Starter

Copy this directory, update the book metadata and chapter list in `_quarto.yml`, then edit the chapter files.

Render from this directory:

```sh
quarto render
```

Use `quarto preview` for a local preview.
