# Quarto Website Starter

Copy this directory, update `_quarto.yml`, and add `.qmd` pages as needed.

Render the site from this directory:

```sh
quarto render
```

Preview it locally with `quarto preview`.
