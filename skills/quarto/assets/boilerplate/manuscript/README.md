# Quarto Manuscript Starter

Copy this directory, update manuscript metadata in `_quarto.yml` and `index.qmd`, and add analysis to `analysis.ipynb`.

Render the manuscript from this directory:

```sh
quarto render
```

The included notebook has no executable cells, so no runtime is required for the starter.
