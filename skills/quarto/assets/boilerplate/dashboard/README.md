# Quarto Dashboard Starter

Copy this directory, edit `index.qmd`, and adjust dashboard options in `_quarto.yml`.

Render from this directory:

```sh
quarto render index.qmd
```

Use `quarto preview index.qmd` for a local preview.
